supported_containers:
  nginx-proxy:
    env_vars:
      DOCKER_OPTS:-d -p 80:80 -v /var/run/docker.sock:/tmp/docker.sock:ro
  booking-movie-ticket:
    env_vars:
      VIRTUAL_HOST: bmt.nhitruong.com
  real-estate:
    env_vars:
      VIRTUAL_HOST: real-estate.nhitruong.com
  thisper:
    env_vars:
      VIRTUAL_HOST: thisper.levantine.io
