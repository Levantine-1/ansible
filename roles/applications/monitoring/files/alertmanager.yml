route:
  receiver: default
  group_by: ["alertname"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  # NOTE: placeholder -- swapped for the Zammad ticket-creation webhook once
  # that relay is deployed (roles/applications/zammad/install.yml). Until
  # then alerts fire and resolve in Alertmanager's own UI but don't notify
  # anywhere else.
  - name: default
